<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <group>
        <item android:id="@+id/action_settings" android:title="@string/action_settings"
            android:orderInCategory="100" app:showAsAction="never" />
        <item android:id="@+id/action_logout" android:title="@string/action_logout"
            android:orderInCategory="100" app:showAsAction="never" />

    </group>
    <!--<item
        android:id="@+id/action_nova_mensagem"
        android:title="Enviar Mensagem"
        android:icon="@drawable/ic_action_name"
        app:showAsAction="ifRoom" /> -->
    <item
        android:id="@+id/action_add_amigo"
        android:title="AddAmigo"
        android:icon="@drawable/ic_person_add_white_24dp"
        app:showAsAction="ifRoom" />
</menu>
